# 快速开始指南

## ⚡ 5分钟快速上手

### 1. 环境准备（已完成✓）

所有依赖已安装完成：
- Python 3.10.19
- numpy, umap-learn, hdbscan, scikit-learn, matplotlib, seaborn, pandas, tqdm

### 2. 运行第一次分析

**选项A：单次快速分析（推荐新手）**

```bash
python clustering_analysis.py
```

在提示符处输入 `n`，使用默认参数进行分析。

预计耗时：5-15分钟

**选项B：参数搜索模式**

```bash
python clustering_analysis.py
```

在提示符处输入 `y`，进行参数搜索以接近512个簇的目标。

预计耗时：1-2小时

### 3. 查看结果

分析完成后，结果保存在 `results/run_YYYYMMDD_HHMMSS/` 目录：

```bash
# 查看可视化图表
xdg-open results/run_*/clustering_visualization.png

# 查看文本报告
cat results/run_*/analysis_report.txt

# 查看JSON摘要
cat results/run_*/summary.json
```

## 🎯 针对512簇目标的参数优化

当前默认参数可能得到的簇数较少（10-100个），要接近512个簇，需要调整参数。

### 快速调整方法

编辑 `clustering_analysis.py`，找到 `else:` 分支（约第515行），修改参数：

```python
# 原始参数（产生较少簇）
clustering.umap_dimensionality_reduction(n_components=75)
clustering.hdbscan_clustering(min_cluster_size=100, min_samples=10)

# 调整为以下参数（产生更多簇）
clustering.umap_dimensionality_reduction(n_components=100)
clustering.hdbscan_clustering(min_cluster_size=30, min_samples=5)
```

### 使用高级参数搜索（推荐）

```bash
python advanced_parameter_search.py
```

该脚本会自动搜索最佳参数组合，寻找接近512个簇的设置。

## 📊 预期结果

### 典型输出示例

```
步骤 3: HDBSCAN聚类

✓ HDBSCAN聚类完成
  - 簇数: 487
  - 噪声点数: 1242 (1.49%)
  - 耗时: 45.32 秒

步骤 4: 聚类质量评估

✓ Silhouette Score: 0.3521
  (范围: [-1, 1], 越接近1越好)
✓ Davies-Bouldin Index: 1.2341
  (越小越好，0为理想值)
✓ Calinski-Harabasz Index: 1234.56
  (越大越好)
```

## 🔧 常见调整场景

### 场景1: 簇数太少（< 200）

```python
# 减小这两个参数
min_cluster_size=50 → 30
min_samples=10 → 5
```

### 场景2: 簇数太多（> 800）

```python
# 增大这两个参数
min_cluster_size=30 → 80
min_samples=5 → 15
```

### 场景3: 噪声点太多（> 10%）

```python
# 增大 min_samples
min_samples=5 → 10 → 15
```

### 场景4: 想要更精细的簇划分

```python
# 增加UMAP降维维度
n_components=50 → 75 → 100

# 减小簇大小阈值
min_cluster_size=100 → 50 → 30
```

## 💡 实用技巧

### 1. 并行运行多个参数组合

打开多个终端，分别运行不同参数：

**终端1:**
```python
# 修改 clustering_analysis.py
n_components=50, min_cluster_size=30, min_samples=5
```

**终端2:**
```python
# 修改 clustering_analysis.py
n_components=75, min_cluster_size=50, min_samples=10
```

**终端3:**
```python
# 修改 clustering_analysis.py
n_components=100, min_cluster_size=80, min_samples=15
```

### 2. 对比结果

运行多次后，使用对比工具：

```bash
python compare_results.py
```

自动查找并对比所有运行结果。

### 3. 监控进度

如果分析时间较长，可以在另一个终端监控：

```bash
# 查看最新结果
watch -n 5 'ls -lth results/ | head -n 5'

# 监控CPU使用
htop
```

## 🎓 下一步学习

1. **理解UMAP参数影响**: 阅读 README.md 的"方法说明"部分
2. **优化评估指标**: 观察Silhouette Score如何随参数变化
3. **簇分析**: 提取特定簇的样本进行音乐特征分析
4. **可视化定制**: 修改可视化代码，添加自定义分析

## ❓ 遇到问题？

### 内存不足

```python
# 在 clustering_analysis.py 中添加采样
data_sample = data[::2]  # 使用一半数据
```

### 运行太慢

- 使用更少的参数搜索组合
- 减小数据采样
- 减小UMAP邻居数

### 结果不理想

- 尝试高级参数搜索: `python advanced_parameter_search.py`
- 调整参数空间范围
- 考虑使用不同的距离度量（cosine vs euclidean）

## 📞 获取帮助

参考完整文档：`README.md`

祝您分析顺利！ 🎵
